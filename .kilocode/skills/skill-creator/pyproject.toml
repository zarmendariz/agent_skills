[project]
name = "skill-creator"
version = "1.0.0"
description = "Skill creator utilities for managing skill packages"
requires-python = ">=3.8"
dependencies = [
    "pyyaml>=6.0",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
